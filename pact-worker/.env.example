# Copy to ".env" and fill in real values. Never commit .env.
PACT_USER=CArahul
PACT_PASSWORD=your-real-pact-password
# true = fill everything but STOP before Post (safe). false = actually post.
DRY_RUN=true
